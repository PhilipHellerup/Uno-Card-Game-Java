package cards; // Project Organization

/* --- Color Enum --- */
public enum Color {
    // Enum representing the four possible Uno card colors.
    // Wild cards will use the value NONE, since they have no color.
    RED, BLUE, GREEN, YELLOW, NONE
}
